{
  "name": "threejs-ts-sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "yarn transpile-server && yarn transpile-client && node dist/server/server",
    "transpile-client": "tsc -p ./src/client",
    "transpile-server": "tsc -p ./src/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "three": "^0.124.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.19",
    "typescript": "^4.1.3"
  }
}
