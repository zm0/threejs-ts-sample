{
  "name": "threejs-ts-sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k \"yarn transpile-server:watch\" \"yarn transpile-client:watch\" \"yarn start:nodemon\"",
    "start:nodemon": "nodemon dist/server/server",
    "transpile-client:watch": "tsc -p ./src/client -w",
    "transpile-server:watch": "tsc -p ./src/server -w",
    "start": "yarn transpile-server && yarn transpile-client && node dist/server/server",
    "transpile-client": "tsc -p ./src/client",
    "transpile-server": "tsc -p ./src/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "three": "^0.124.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.19",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6",
    "typescript": "^4.1.3"
  }
}
